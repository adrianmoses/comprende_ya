# API Keys - Required for the application to run
ANTHROPIC_API_KEY=sk-ant-xxx
OPENAI_API_KEY=sk-xxx
YOUTUBE_API_KEY=your_youtube_api_key_here

# Database (these are set in docker-compose.yaml, but can be overridden)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/comprende_ya
# DATABASE_ECHO=false

# Prefect (optional, defaults set in docker-compose.yaml)
# PREFECT_API_URL=http://prefect:4200/api
